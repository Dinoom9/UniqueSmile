import DentalForm from './components/DentalForm';

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gradient-to-b from-lightBlue to-white text-primaryText">
      <DentalForm />
    </main>
  );
}
